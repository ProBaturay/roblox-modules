Ever noticed you had so many scripts or SLOC and needed to handle it?

This plugin essentially works for the existing scripts in the DataModel, except for the core scripts.

Clean, fresh and modern UI
